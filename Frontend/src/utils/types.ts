export type Wine = {
    name: string
    vintage: string
    price: string;
    quantity: string;
    origin: string;
  };

export interface WineList {
  listName: string;
  creationDate: string;
  link: string;
}
